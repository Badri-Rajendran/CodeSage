#cloud-config
# Bootstraps a fresh Ubuntu droplet with Docker Engine + Compose plugin and a
# non-root deploy user. Passed as --user-data when creating the droplet.
package_update: true
package_upgrade: true

packages:
  - ca-certificates
  - curl
  - git
  - ufw

users:
  - name: deploy
    groups: [docker, sudo]
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]

runcmd:
  # Install Docker Engine + Compose plugin from Docker's official repo.
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  - chmod a+r /etc/apt/keyrings/docker.asc
  - echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" > /etc/apt/sources.list.d/docker.list
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  - systemctl enable --now docker
  # Firewall: SSH + HTTP/HTTPS only.
  - ufw allow OpenSSH
  - ufw allow 80/tcp
  - ufw allow 443/tcp
  - ufw --force enable
  - usermod -aG docker deploy
  - mkdir -p /opt/codesage && chown deploy:deploy /opt/codesage
